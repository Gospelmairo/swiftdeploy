network:
  driver_type: bridge
  name: swiftdeploy-net
nginx:
  image: nginx:latest
  port: 8080
services:
  image: swift-deploy-1-node:latest
  mode: canary
  port: 3000
  proxy_timeout: 30
  restart_policy: unless-stopped
  version: 1.0.0
